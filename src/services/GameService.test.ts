import {describe, it, expect} from 'vitest'

describe('GameService', () => {

    it('doit afficher "moins" si la carte donnée par l\'utilisateur est en-dessous de celle à deviner', () => {
        const gameService = new GameServiceTest();

    });
});